package com.github.hartorn.dd.webapp.model.generated;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: com.lejeu.ideo.model.generated.GroupeDroitAssoc
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class GroupeDroitAssocId
    implements Serializable
{
    static
    {
        // register persistent class in JVM
        try { Class.forName ("com.lejeu.ideo.model.generated.GroupeDroitAssoc"); }
        catch (Exception e) {}
    }

    public int droit;
    public int groupe;

    public GroupeDroitAssocId ()
    {
    }

    public GroupeDroitAssocId (String str)
    {
        fromString (str);
    }

    public int getDroit ()
    {
        return droit;
    }

    public void setDroit (int droit)
    {
        this.droit = droit;
    }

    public int getGroupe ()
    {
        return groupe;
    }

    public void setGroupe (int groupe)
    {
        this.groupe = groupe;
    }

    public String toString ()
    {
        return String.valueOf (droit)
            + "::" + String.valueOf (groupe);
    }

    public int hashCode ()
    {
        int rs = 17;
        rs = rs * 37 + droit;
        rs = rs * 37 + groupe;
        return rs;
    }

    public boolean equals (Object obj)
    {
        if (this == obj)
            return true;
        if (obj == null || obj.getClass () != getClass ())
            return false;

        GroupeDroitAssocId other = (GroupeDroitAssocId) obj;
        return (droit == other.droit)
            && (groupe == other.groupe);
    }

    private void fromString (String str)
    {
        Tokenizer toke = new Tokenizer (str);
        str = toke.nextToken ();
        droit = Integer.parseInt (str);
        str = toke.nextToken ();
        groupe = Integer.parseInt (str);
    }

    protected static class Tokenizer
    {
        private final String str;
        private int last;

        public Tokenizer (String str)
        {
            this.str = str;
        }

        public String nextToken ()
        {
            int next = str.indexOf ("::", last);
            String part;
            if (next == -1)
            {
                part = str.substring (last);
                last = str.length ();
            }
            else
            {
                part = str.substring (last, next);
                last = next + 2;
            }
            return part;
        }
    }
}