package com.github.hartorn.dd.webapp.model.generated;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: com.lejeu.ideo.model.generated.IrlParticipation
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class IrlParticipationId
    implements Serializable
{
    static
    {
        // register persistent class in JVM
        try { Class.forName ("com.lejeu.ideo.model.generated.IrlParticipation"); }
        catch (Exception e) {}
    }

    public long irl;
    public int perso;

    public IrlParticipationId ()
    {
    }

    public IrlParticipationId (String str)
    {
        fromString (str);
    }

    public long getIrl ()
    {
        return irl;
    }

    public void setIrl (long irl)
    {
        this.irl = irl;
    }

    public int getPerso ()
    {
        return perso;
    }

    public void setPerso (int perso)
    {
        this.perso = perso;
    }

    public String toString ()
    {
        return String.valueOf (irl)
            + "::" + String.valueOf (perso);
    }

    public int hashCode ()
    {
        int rs = 17;
        rs = rs * 37 + (int) (irl ^ (irl >>> 32));
        rs = rs * 37 + perso;
        return rs;
    }

    public boolean equals (Object obj)
    {
        if (this == obj)
            return true;
        if (obj == null || obj.getClass () != getClass ())
            return false;

        IrlParticipationId other = (IrlParticipationId) obj;
        return (irl == other.irl)
            && (perso == other.perso);
    }

    private void fromString (String str)
    {
        Tokenizer toke = new Tokenizer (str);
        str = toke.nextToken ();
        irl = Long.parseLong (str);
        str = toke.nextToken ();
        perso = Integer.parseInt (str);
    }

    protected static class Tokenizer
    {
        private final String str;
        private int last;

        public Tokenizer (String str)
        {
            this.str = str;
        }

        public String nextToken ()
        {
            int next = str.indexOf ("::", last);
            String part;
            if (next == -1)
            {
                part = str.substring (last);
                last = str.length ();
            }
            else
            {
                part = str.substring (last, next);
                last = next + 2;
            }
            return part;
        }
    }
}