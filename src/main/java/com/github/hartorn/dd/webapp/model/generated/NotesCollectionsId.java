package com.github.hartorn.dd.webapp.model.generated;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: com.lejeu.ideo.model.generated.NotesCollections
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class NotesCollectionsId
    implements Serializable
{
    static
    {
        // register persistent class in JVM
        try { Class.forName ("com.lejeu.ideo.model.generated.NotesCollections"); }
        catch (Exception e) {}
    }

    public int id;
    public int perso;

    public NotesCollectionsId ()
    {
    }

    public NotesCollectionsId (String str)
    {
        fromString (str);
    }

    public int getId ()
    {
        return id;
    }

    public void setId (int id)
    {
        this.id = id;
    }

    public int getPerso ()
    {
        return perso;
    }

    public void setPerso (int perso)
    {
        this.perso = perso;
    }

    public String toString ()
    {
        return String.valueOf (id)
            + "::" + String.valueOf (perso);
    }

    public int hashCode ()
    {
        int rs = 17;
        rs = rs * 37 + id;
        rs = rs * 37 + perso;
        return rs;
    }

    public boolean equals (Object obj)
    {
        if (this == obj)
            return true;
        if (obj == null || obj.getClass () != getClass ())
            return false;

        NotesCollectionsId other = (NotesCollectionsId) obj;
        return (id == other.id)
            && (perso == other.perso);
    }

    private void fromString (String str)
    {
        Tokenizer toke = new Tokenizer (str);
        str = toke.nextToken ();
        id = Integer.parseInt (str);
        str = toke.nextToken ();
        perso = Integer.parseInt (str);
    }

    protected static class Tokenizer
    {
        private final String str;
        private int last;

        public Tokenizer (String str)
        {
            this.str = str;
        }

        public String nextToken ()
        {
            int next = str.indexOf ("::", last);
            String part;
            if (next == -1)
            {
                part = str.substring (last);
                last = str.length ();
            }
            else
            {
                part = str.substring (last, next);
                last = next + 2;
            }
            return part;
        }
    }
}