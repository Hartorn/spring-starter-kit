package com.github.hartorn.dd.webapp.model.generated;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: com.lejeu.ideo.model.generated.SortAssoc
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class SortAssocId
    implements Serializable
{
    static
    {
        // register persistent class in JVM
        try { Class.forName ("com.lejeu.ideo.model.generated.SortAssoc"); }
        catch (Exception e) {}
    }

    public int sort;
    public int type;

    public SortAssocId ()
    {
    }

    public SortAssocId (String str)
    {
        fromString (str);
    }

    public int getSort ()
    {
        return sort;
    }

    public void setSort (int sort)
    {
        this.sort = sort;
    }

    public int getType ()
    {
        return type;
    }

    public void setType (int type)
    {
        this.type = type;
    }

    public String toString ()
    {
        return String.valueOf (sort)
            + "::" + String.valueOf (type);
    }

    public int hashCode ()
    {
        int rs = 17;
        rs = rs * 37 + sort;
        rs = rs * 37 + type;
        return rs;
    }

    public boolean equals (Object obj)
    {
        if (this == obj)
            return true;
        if (obj == null || obj.getClass () != getClass ())
            return false;

        SortAssocId other = (SortAssocId) obj;
        return (sort == other.sort)
            && (type == other.type);
    }

    private void fromString (String str)
    {
        Tokenizer toke = new Tokenizer (str);
        str = toke.nextToken ();
        sort = Integer.parseInt (str);
        str = toke.nextToken ();
        type = Integer.parseInt (str);
    }

    protected static class Tokenizer
    {
        private final String str;
        private int last;

        public Tokenizer (String str)
        {
            this.str = str;
        }

        public String nextToken ()
        {
            int next = str.indexOf ("::", last);
            String part;
            if (next == -1)
            {
                part = str.substring (last);
                last = str.length ();
            }
            else
            {
                part = str.substring (last, next);
                last = next + 2;
            }
            return part;
        }
    }
}