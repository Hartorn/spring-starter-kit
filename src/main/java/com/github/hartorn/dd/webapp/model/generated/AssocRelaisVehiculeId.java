package com.github.hartorn.dd.webapp.model.generated;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: com.lejeu.ideo.model.generated.AssocRelaisVehicule
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class AssocRelaisVehiculeId
    implements Serializable
{
    static
    {
        // register persistent class in JVM
        try { Class.forName ("com.lejeu.ideo.model.generated.AssocRelaisVehicule"); }
        catch (Exception e) {}
    }

    public int idrelais;
    public int idvehicule;

    public AssocRelaisVehiculeId ()
    {
    }

    public AssocRelaisVehiculeId (String str)
    {
        fromString (str);
    }

    public int getIdrelais ()
    {
        return idrelais;
    }

    public void setIdrelais (int idrelais)
    {
        this.idrelais = idrelais;
    }

    public int getIdvehicule ()
    {
        return idvehicule;
    }

    public void setIdvehicule (int idvehicule)
    {
        this.idvehicule = idvehicule;
    }

    public String toString ()
    {
        return String.valueOf (idrelais)
            + "::" + String.valueOf (idvehicule);
    }

    public int hashCode ()
    {
        int rs = 17;
        rs = rs * 37 + idrelais;
        rs = rs * 37 + idvehicule;
        return rs;
    }

    public boolean equals (Object obj)
    {
        if (this == obj)
            return true;
        if (obj == null || obj.getClass () != getClass ())
            return false;

        AssocRelaisVehiculeId other = (AssocRelaisVehiculeId) obj;
        return (idrelais == other.idrelais)
            && (idvehicule == other.idvehicule);
    }

    private void fromString (String str)
    {
        Tokenizer toke = new Tokenizer (str);
        str = toke.nextToken ();
        idrelais = Integer.parseInt (str);
        str = toke.nextToken ();
        idvehicule = Integer.parseInt (str);
    }

    protected static class Tokenizer
    {
        private final String str;
        private int last;

        public Tokenizer (String str)
        {
            this.str = str;
        }

        public String nextToken ()
        {
            int next = str.indexOf ("::", last);
            String part;
            if (next == -1)
            {
                part = str.substring (last);
                last = str.length ();
            }
            else
            {
                part = str.substring (last, next);
                last = next + 2;
            }
            return part;
        }
    }
}